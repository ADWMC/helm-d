- insert:
    - id: helmd-health
      name: 'helm-d'
    # H-CoT settings namespace + UI data plane. Its own Entry so two namespaces
    # never share one apply (and one registration cannot shadow the other).
    - id: helmd-hcot-settings
      name: 'helm-d/hcot-settings'
